apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "broadcast.fullname" . }}-test-connection"
  labels:
    {{- include "broadcast.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "broadcast.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
